<% include ./partials/header.ejs %>

    <div class='row' style='text-align:center;'>
        <h1>
            Welcome <%= user.username %>!
        </h1>
        <div style='width:30%; margin: 40px auto;'>
            <div>
                <form action="/<%= user.id %>/floorplan/" method="GET">
                    <button class='btn btn-primary btn-lg btn-block'> Floorplans </button>
                    <a href= "/<%= user.id %>/floorplans"></a>
                </form>
            </div>
            <br>
            <div>
                <button class='btn btn-primary btn-lg btn-block'> Emergency </button>
            </div>
        </div>

<% include ./partials/footer.ejs %>